import SwiftUI

struct ActionView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("Action (Coming soon)").foregroundColor(.white)
        }
    }
}
