import SwiftUI

struct MehrView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("Mehr (Coming soon)").foregroundColor(.white)
        }
    }
}
