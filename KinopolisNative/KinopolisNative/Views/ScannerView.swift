import SwiftUI

struct ScannerView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("Scanner (Coming soon)").foregroundColor(.white)
        }
    }
}
